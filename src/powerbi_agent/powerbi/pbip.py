"""PBIP project writer.

Writes ``<Name>.pbip``, ``<Name>.SemanticModel/`` (definition.pbism + definition/) and
``<Name>.Report/`` (definition.pbir with a byPath reference to the semantic model).
Note: the project file extension is ``.pbip``, not ``.pbiproj``.

Not implemented yet: scheduled for Phase 6.
"""
